extends:
  - spectral:oas
  - spectral:asyncapi

rules:
  # The fastchat spec has rich descriptions everywhere; enforce that the bar stays high.
  operation-description: warn
  operation-tag-defined: warn
  oas3-server-not-example.com: off
  info-contact: off
  info-license: off
